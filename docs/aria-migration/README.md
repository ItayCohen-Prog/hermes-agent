# Aria Migration Docs

This directory freezes the current Aria bridge behavior while Hermes absorbs Aria.

Scope:
- document current runtime services, ports, commands, events, cron jobs, and compatibility requirements;
- guide incremental migration from `/home/aria/aria-bridge` to Hermes profile/plugin/tooling;
- keep current Aria live until Hermes parity is verified.

Security rule: do not include secrets, tokens, API keys, passwords, private keys, real `.env` values, or credential file contents. Use `[REDACTED]` for any secret-like material.
